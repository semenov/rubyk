- if !@comments.empty?
  .comments
    - for comment in @comments
      .post
        .content
          = markdown h comment.post.content    
        = render :partial => "comment", :locals => { :comment => comment}
  = will_paginate(@comments)
- else
  %p Пока комментариев нет.
